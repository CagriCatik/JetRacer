Place the Vosk speech model directory here.

Expected default path:

- `share/jetracer_voice/models/vosk-model-small-en-us-0.15`

The repository does not vendor the model weights. Download the model separately and
extract it under this directory before launching `voice_commander.py`.
